rootProject.name = "Mountainspotter"

pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

include(":shared")
include(":shared-core")
include(":androidApp")
include(":iosApp")
